{
  "name": "@nakamiri/kiro-cli-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use Kiro CLI from Claude Code to review code or delegate tasks.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "22.12.0",
    "typescript": "^5.7.0"
  }
}
